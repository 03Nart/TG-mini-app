/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['127.0.0.1', 'd.lazaristcatholicschool.org',], // All domains in one array
    },
  
  };
  
  export default nextConfig;
  